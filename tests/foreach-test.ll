int[] as
as[] = 5 // a[0]
as[] = 6 // a[1]

foreach x in as :
    print x
print ""

foreach i in as :
    foreach j in as:
        foreach k in as :
            print as + " " + i + " " + j + " " + k
